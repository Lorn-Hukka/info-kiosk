<UserControl x:Class="InfoKiosk.Modules.Survey.Views.KeyBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InfoKiosk.Modules.Survey.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid x:Name="Grid_Closed_Like" Visibility="Hidden">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Bardzo"/>
            <Button Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Średnio"/>
            <Button Grid.Column="2"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Niezbyt"/>
            <Button Grid.Column="3"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Nie"/>
        </Grid>
        <Grid x:Name="Grid_Closed_Stance" Visibility="Hidden">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Tak"/>
            <Button Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Nie mam zdania"/>
            <Button Grid.Column="2"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Nie"/>
        </Grid>
        <Grid x:Name="Grid_Closed_Survey" Visibility="Visible">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Name="Previous_survey"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Poprzednia ankieta" Click="Previous_survey_Click"/>
            <Button Grid.Column="1"
                    Name="Select_survey"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Wybierz" Click="Select_survey_Click"/>
            <Button Grid.Column="2"
                    Name="Next_survey"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Padding="20"
                    FontSize="20"
                    Margin="5"
                    Content="Nastepna ankieta" Click="Next_survey_Click"/>
        </Grid>
        <Grid x:Name="Grid_Open_Keyboard"
              Visibility="Hidden">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBox
                Name="textBox"
                MinHeight="25"
                Grid.Column="1"
                HorizontalAlignment="Stretch"
                Margin="10"
                VerticalAlignment="Center"
                FontSize="14" />
            <Button Content=" "
                    Grid.Column="1"
                    Grid.Row="2"
                    HorizontalAlignment="Stretch"
                    Height="60"
                    Click="Button_Click"
                    VerticalAlignment="Center"
                    Margin="10" />
            <Button
                Content=" Następne pytanie"
                Grid.Column="2"
                Grid.Row="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="20"
                Click="Button_Click_Answer" />
            
            <ItemsControl Grid.ColumnSpan="3"
                          Grid.Column="0" Grid.Row="1"
                          Name="KeyboardGrid"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Margin="0,0,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="12"
                                     Rows="4"
                                     Margin="5"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Content}"
                                Width="50"
                                Height="50"
                                Click="Button_Click"
                                Margin="2"
                                FontSize="16"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
